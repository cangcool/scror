<%= form_for(@task_position_person) do |f| %>
  <% if @task_position_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_position_person.errors.count, "error") %> prohibited this task_position_person from being saved:</h2>

      <ul>
      <% @task_position_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_position_id %><br>
    <%= f.number_field :task_position_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :assigner_id %><br>
    <%= f.number_field :assigner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
