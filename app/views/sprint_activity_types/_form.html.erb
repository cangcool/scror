<%= form_for(@sprint_activity_type) do |f| %>
  <% if @sprint_activity_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sprint_activity_type.errors.count, "error") %> prohibited this sprint_activity_type from being saved:</h2>

      <ul>
      <% @sprint_activity_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :format %><br>
    <%= f.text_field :format %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
