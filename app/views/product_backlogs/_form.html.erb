<%= form_for(@product_backlog) do |f| %>
  <% if @product_backlog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_backlog.errors.count, "error") %> prohibited this product_backlog from being saved:</h2>

      <ul>
      <% @product_backlog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :sprint_id %><br>
    <%= f.number_field :sprint_id %>
  </div>
  <div class="field">
    <%= f.label :point %><br>
    <%= f.number_field :point %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
