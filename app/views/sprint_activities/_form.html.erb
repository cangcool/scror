<%= form_for(@sprint_activity) do |f| %>
  <% if @sprint_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sprint_activity.errors.count, "error") %> prohibited this sprint_activity from being saved:</h2>

      <ul>
      <% @sprint_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :spint_id %><br>
    <%= f.number_field :spint_id %>
  </div>
  <div class="field">
    <%= f.label :sprint_activity_type_id %><br>
    <%= f.number_field :sprint_activity_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
