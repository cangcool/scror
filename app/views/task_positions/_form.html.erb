<%= form_for(@task_position) do |f| %>
  <% if @task_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_position.errors.count, "error") %> prohibited this task_position from being saved:</h2>

      <ul>
      <% @task_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %><br>
    <%= f.number_field :task_id %>
  </div>
  <div class="field">
    <%= f.label :sprint_board_column_id %><br>
    <%= f.number_field :sprint_board_column_id %>
  </div>
  <div class="field">
    <%= f.label :is_block %><br>
    <%= f.check_box :is_block %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
