<%= form_for(@default_board_column) do |f| %>
  <% if @default_board_column.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@default_board_column.errors.count, "error") %> prohibited this default_board_column from being saved:</h2>

      <ul>
      <% @default_board_column.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :boardcolumn_type_id %><br>
    <%= f.number_field :boardcolumn_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
