<%= form_for(@sprint_task_type) do |f| %>
  <% if @sprint_task_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sprint_task_type.errors.count, "error") %> prohibited this sprint_task_type from being saved:</h2>

      <ul>
      <% @sprint_task_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sprint_id %><br>
    <%= f.number_field :sprint_id %>
  </div>
  <div class="field">
    <%= f.label :color_code %><br>
    <%= f.text_field :color_code %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
